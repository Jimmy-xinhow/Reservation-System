{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "node --check scripts/trigger-reminders.mjs"
  },
  "deploy": {
    "startCommand": "node scripts/trigger-reminders.mjs --jobs=subscription-freezes",
    "cronSchedule": "5 16 * * *",
    "restartPolicyType": "NEVER"
  }
}
