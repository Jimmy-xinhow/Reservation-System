{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "node --check scripts/run-allowlisted-cron.mjs"
  },
  "deploy": {
    "startCommand": "node scripts/run-allowlisted-cron.mjs --jobs=subscription-freezes",
    "cronSchedule": "5 16 * * *",
    "restartPolicyType": "NEVER"
  }
}
