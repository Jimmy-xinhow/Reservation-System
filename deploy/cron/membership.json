{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "node --check scripts/run-allowlisted-cron.mjs"
  },
  "deploy": {
    "startCommand": "node scripts/run-allowlisted-cron.mjs --jobs=membership",
    "cronSchedule": "45 * * * *",
    "restartPolicyType": "NEVER"
  }
}
