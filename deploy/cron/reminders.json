{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "node --check scripts/run-allowlisted-cron.mjs"
  },
  "deploy": {
    "startCommand": "node scripts/run-allowlisted-cron.mjs --jobs=reminders",
    "cronSchedule": "0 * * * *",
    "restartPolicyType": "NEVER"
  }
}
