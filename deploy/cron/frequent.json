{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "node --check scripts/trigger-reminders.mjs"
  },
  "deploy": {
    "startCommand": "node scripts/trigger-reminders.mjs --jobs=followups,registration,richmenu",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
