{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "node --check scripts/trigger-reminders.mjs"
  },
  "deploy": {
    "startCommand": "node scripts/trigger-reminders.mjs --jobs=marketing",
    "cronSchedule": "30 * * * *",
    "restartPolicyType": "NEVER"
  }
}
