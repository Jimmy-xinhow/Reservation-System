# 預約與報名 SaaS 平台 — 技術規格 v3

> 版本：v3　狀態：開發與驗收基準　日期：2026-08-05
>
> 本文件由服務簡報與已確認的產品決策整理而成。開發、資料庫遷移、測試與正式驗收以本文件及 `AGENTS.md`／`CLAUDE.md` 為準；`clinic-booking-spec-v2.md` 與 `saas-platform-plan-v1.md` 僅保留作為歷史紀錄，不得再作為開發依據。

---

## 1. 已確認的產品決策

| 項目 | v3 決定 |
|---|---|
| 產品方向 | 以可持續擴充的多品牌 SaaS 為目標；現有 `clinic_id` 保留作為相容的租戶識別鍵。 |
| 核心流程 | 預約與課程／活動報名同時納入，使用同一套帳號、權限、名額、通知與稽核邊界。 |
| 方案範圍 | 簡報列出的 70 項功能全數開放，不依預約筆數、使用人數或功能等級加價。 |
| 金流 | 綠界、藍新的標準金流串接納入；其他金流、完整退款與對帳流程另行報價。系統不代收款。 |
| 行銷 | 納入依規則執行的分眾行銷與 CRM Lite；不做完整 CRM、AI 行銷自動化。 |
| CRM Lite | 顧客資料、標籤／分眾、互動時間軸、預約完成追蹤、生日與未回訪提醒。 |
| 統計 | 納入營運報表，至少包含預約量、報名量、未到率、候補填補率與可匯出資料。 |
| 品牌與人員 | 品牌資料完全隔離；支援擁有者、管理員、櫃檯、服務提供者等多人員角色。 |
| 入口 | 主要入口為 LINE Rich Menu → LIFF；另保留瀏覽器、Email、自訂網址、嵌入元件與自訂網域。 |
| 加值項目 | 七項加值項目另行報價；清單外需求可評估客製。 |
| 維護界線 | 合約提供系統維護、錯誤修正、安全性與相容性維護，不包含建立新功能；新功能與客製需求另行報價。 |

## 2. 產品範圍與明確不做

### 2.1 標準方案包含

1. 預約管理：時間制與號次制、服務／人員／資源、門診或服務時段、容量、候補、取消、改期與提醒。
2. 課程與活動報名：活動、課程、梯次、場次、票種、自訂表單、報名截止、候補、QR 報到與名單匯出。
3. 顧客與會員：基本資料、登入／身分識別、會員資料、套票／堂數與優惠碼等簡報列出的功能。
4. 標準收款：綠界／藍新付款建立、付款結果回呼、訂金狀態與付款完成後確認名額。
5. 通知：LINE Messaging API、LIFF、Rich Menu、Email 通知、預約／報名確認、取消、改期、候補與行前提醒。
6. CRM Lite：顧客資料、分眾規則、互動時間軸、規則式自動化與投遞紀錄。
7. 後台與報表：成員、角色、品牌設定、預約／報名／報到管理、營運統計與匯出。
8. 品牌入口：共享預約網址、自訂網址、瀏覽器備援、官網嵌入元件與自訂網域設定位置。

### 2.2 不在標準方案

- 健保掛號、HIS／病歷同步、醫療資料交換。
- 即時叫號與看診進度系統。
- 完整 CRM（例如複雜銷售管線、客服工單、企業客戶管理）。
- AI 內容生成、AI 自動分群或 AI 行銷流程。
- 評價導流、公開活動搜尋市集、廣告投放與流量分潤。
- 綠界／藍新以外的指定金流商、完整退款與對帳流程。
- 合約內新增未列入 70 項的功能；可另行評估、報價與排程。

### 2.3 七項另行報價

1. 指定金流商串接。
2. 退款與對帳流程。
3. 行事曆雙向同步。
4. API 與 Webhook 對外串接。
5. 白標。
6. 多語系介面。
7. 特殊產業流程客製，例如初複診、療程管理、分院或分店架構。

---

## 3. 技術與部署基線

- Next.js 15 App Router、TypeScript `strict: true`、React 19、Tailwind CSS v4。
- Supabase Postgres + Auth；不引入 ORM，不為單一功能更換框架。
- LINE Messaging API、LIFF；LINE webhook 必須驗證簽章。
- Vercel Cron 或現有排程執行提醒與行銷自動化；Cron 使用 UTC，程式內統一轉 `Asia/Taipei`。
- Email 由 server-only provider 設定發送；provider、寄件網域與金鑰屬部署設定，不寫入前端。
- 所有業務時間欄位使用 `timestamptz`；顯示與日期規則以租戶時區為準，首版預設 `Asia/Taipei`。
- 所有租戶／品牌業務資料保留 `clinic_id` 作為相容租戶鍵；不得在未完成遷移與回滾驗證前直接改名或刪除。

### 3.1 請求邊界

1. 顧客端（LIFF、瀏覽器、嵌入元件）只能呼叫本專案 Next.js API route。
2. 顧客端永不直接連 Supabase；`SUPABASE_SERVICE_ROLE_KEY` 只可在 server 使用。
3. 後台使用 Supabase Auth；所有資料表啟用 RLS，anon 不得取得任何顧客資料。
4. 服務端每一次查詢、寫入、外部呼叫都必須帶租戶範圍並處理錯誤。
5. 不得依 URL、前端傳入的 `clinic_id` 或 `line_user_id` 直接信任租戶與身分；需由 server session／驗證 token 決定。

---

## 4. SaaS 租戶、品牌與帳號

### 4.1 租戶模型

- 產品層稱「品牌／租戶」，資料庫現階段沿用 `clinic_id` 作為租戶鍵。
- 同一個登入帳號可以被加入多個品牌；後台每次請求必須有明確的 active brand context。
- 所有業務表至少包含 `clinic_id`，RLS、server query、API response、匯出檔都必須套用該欄位的隔離條件。
- 品牌停用使用 `active=false` 或等價 soft-delete；不得硬刪除有歷史預約、報名、付款或互動的品牌。
- 新建品牌時自動建立預設設定、預設角色權限與可用的空白 LINE／Email 設定。

### 4.2 角色與權限

| 角色 | 可做事項 | 不可做事項 |
|---|---|---|
| 擁有者 | 品牌設定、成員、權限、金流／通知設定、全部營運資料 | 不可存取其他品牌資料，除非另有該品牌成員資格。 |
| 管理員 | 預約、報名、顧客、報表、訊息、排程與大部分設定 | 不可移除擁有者或變更平台層設定。 |
| 櫃檯／營運 | 建立與管理預約、報名、候補、報到、顧客資料與必要匯出 | 不可讀取不必要的金流密鑰與平台設定。 |
| 服務提供者／講師 | 查看被分配的行程、必要的顧客／學員資訊、執行報到或完成狀態 | 不可讀取其他品牌或未授權的完整顧客名單。 |

角色矩陣要在後台與 RLS／server authorization 同時落實，不能只依前端隱藏按鈕。

---

## 5. 預約與報名規格

### 5.1 預約

- `booking_mode=time`：顧客選確切日期與時間；依服務時長、緩衝時間與區間重疊計算容量。
- `booking_mode=number`：顧客選日期與診次／場次；系統依整診容量發下一個號碼，取消的號碼不重用。
- 時段只顯示未來且未滿的選項；最短前置時間與最長預約天數由設定控制，預設 30 分鐘／30 天。
- 同一服務提供者一天可有多個時段；不得用 `limit 1` 只取一筆模板。
- 初診／複診時長由設定控制；初診延長時用區間重疊避免超賣。
- 名額計算以 `booked`、`confirmed`、`done` 佔位；`cancelled`、`no_show` 不佔位，除非產品規則另行定義。
- 訂位、候補遞補、付款確認等會消耗名額的動作必須在 server／SQL transaction 原子化。
- 取消採狀態變更，不 DELETE；改期需保留原紀錄與操作者、時間、原因。
- 一電話多顧客由租戶設定控制；電話不可設全域 unique。

### 5.2 課程與活動報名

報名不可硬塞進 `appointments`，需建立獨立領域資料表，至少包含：

- `events`：活動／課程基本資料、品牌、公開狀態、報名期間、說明。
- `event_sessions`：梯次／場次、開始／結束時間、地點、容量與候補設定。
- `event_ticket_types`：免費／付費票種、價格、名額、套票／優惠關聯。
- `registration_forms`、`registration_form_fields`：版本化表單定義與必要欄位。
- `registrations`、`registration_answers`：報名主檔、狀態、付款狀態與送出時的答案快照。
- `waitlist_entries`：候補順序、遞補狀態與通知紀錄。
- `checkins`：QR 報到憑證、報到時間、操作者與重複掃描結果。

報名驗收規則：

1. 報名頁可公開或私密；可設定截止時間、票種、表單與名額。
2. 額滿後不可繼續建立有效報名；候補依序遞補且不得重複通知。
3. 已送出的表單必須保留當時版本的題目與答案，不受後續編輯影響。
4. QR 憑證不可直接包含姓名、電話或可逆個資；無效、過期、重複掃描要有明確結果。
5. 報名取消、付款失敗、候補遞補與報到狀態都必須可追溯。

---

## 6. 標準金流規格

### 6.1 納入範圍

- 綠界、藍新標準付款流程。
- 每個品牌各自保存金流必要設定；密鑰只能在 server-only secret 使用。
- 建立付款訂單、綁定預約／報名、接收付款回呼、驗證簽章與更新付款狀態。
- 付款成功後才確認需要付款的預約／報名；付款失敗或逾時釋放保留名額。
- 訂金設定仍由租戶規則決定，付款結果與預約／報名狀態分開保存。

### 6.2 不納入標準金流

- 其他金流商。
- 自動退款、部分退款、退款審批、完整對帳與會計／發票流程。
- 以上需求列入加值項目，不得在維護合約內默默加入。

### 6.3 必要資料與稽核

至少建立 `payment_orders`、`payment_transactions`、必要的 webhook event log；每筆資料帶 `clinic_id`、外部訂單號、金額、幣別、狀態、原始回呼識別與時間。回呼必須具備冪等處理，重送不得重複確認名額或重複寫入交易。

---

## 7. CRM Lite 與規則式行銷自動化

### 7.1 CRM Lite 標準功能

- 顧客基本資料：姓名、電話、LINE 身分、Email、生日、標籤、備註、行銷同意與封鎖狀態。
- 分眾規則：標籤包含、未回訪天數、完成次數、未到次數、生日月份等可解釋的規則。
- 顧客互動時間軸：預約、報名、訊息、行銷投遞、櫃檯備註與系統事件。
- 後台可查看分眾人數、刷新分眾、查看顧客明細與停用分眾。

### 7.2 自動化觸發

標準自動化限定以下三種：

1. `appointment_done`：預約完成／結束後追蹤。
2. `birthday`：生日訊息。
3. `inactive`：達到設定天數未回訪，且沒有未來有效預約。

每個自動化可選 LINE 或 Email、延遲、觸發天數、冷卻天數、標題／內容與適用分眾。行銷訊息必須符合：

- `marketing_opt_in=false` 不得發送。
- 顧客取消同意或被封鎖後，待發訊息要跳過，並記錄 `skipped` 原因。
- 相同自動化、顧客、觸發事件、渠道不得重複發送；失敗可依重試規則重送。
- 每次嘗試、成功、失敗、跳過都保留投遞紀錄。
- 不提供 AI 自動產文、不提供任意節點流程編排；需要時另行報價。

### 7.3 行銷驗收

- 後台可建立、停用、修改與預覽自動化。
- 測試顧客可驗證 opt-in、冷卻、去重、失敗重試與取消同意。
- LINE／Email 外部服務失敗不應使 Cron 整批中斷；錯誤要進投遞紀錄與 server log。
- 自動化 Cron 必須可單獨觸發、可重跑且不重複發送。

---

## 8. 通知、入口與品牌化

- 主要入口：LINE Rich Menu → LIFF。
- LIFF ID token 必須由 server 向 LINE 驗證後才能採用 `line_user_id`。
- webhook 必須先驗證 `x-line-signature`，再依 LINE payload 的 `destination` 對應品牌；多品牌 channel secret／access token 只能由 server environment 的 destination map 取得，不可接受未驗證或無法對應的事件。
- 預約／報名確認、取消、改期、候補、行前提醒與行銷訊息需有渠道與投遞紀錄。
- 提供瀏覽器備援連結；非 LINE 使用者可依品牌允許的欄位完成流程。
- 提供自訂網址、官網嵌入元件與自訂網域的路由／設定位置；網域綁定必須驗證所有權並使用 HTTPS。
- 品牌名稱、色系、識別與公開頁內容不得跨品牌混用。
- 外部成本（LINE 官方帳號／推播、金流手續費、網域年費、Email provider）由品牌方負擔，系統不得宣稱已包含外部費用。

---

## 9. 報表與資料匯出

### 9.1 標準報表

至少提供：

- 預約量：依日期、服務、服務提供者、狀態與來源統計。
- 報名量：依活動、梯次／場次、票種與狀態統計。
- 未到率：有效預約／報名中未到的比例，明確顯示分母。
- 候補填補率：候補成功遞補人數／候補總人數，明確顯示無資料時的結果。
- 付款摘要：標準付款成功／失敗／待付款筆數與金額；不包含完整對帳。
- CRM 基本摘要：分眾人數與自動化投遞成功／失敗／跳過數。

### 9.2 報表共通規則

- 日期計算以品牌時區顯示，資料庫仍使用 `timestamptz`。
- 每份報表需有品牌、日期範圍、最後更新時間與資料範圍說明。
- 匯出 CSV 必須套用相同租戶與角色權限；顧客 PII 僅提供有權限的後台角色。
- 報表不得把已取消資料誤算為有效名額；各指標需有自動化測試案例。

---

## 10. 資料模型、狀態與安全

### 10.1 共通資料規則

- 所有業務表帶 `clinic_id`；外鍵不可讓刪除上層租戶時意外刪除歷史交易、預約、報名與互動。
- 醫師、服務、品牌與成員採 soft-delete 或停用，不硬刪歷史資料。
- `updated_at` 由 trigger 或等價 server 機制維護。
- 預約狀態：`booked / confirmed / cancelled / done / no_show`。
- 報名狀態至少：`pending / confirmed / cancelled / waitlisted / attended / no_show`。
- 付款狀態至少：`pending / paid / failed / expired / refunded`；退款狀態不可冒充付款成功。
- 所有狀態異動需有操作者／來源、時間、原狀態與新狀態；系統事件可標記為 system。

### 10.2 個資與 RLS

1. 所有表開啟 RLS；anon 不得有讀取顧客資料的 policy。
2. 後台查詢使用登入成員與 `clinic_id` 的 policy／server authorization。
3. 顧客端只回傳完成當前流程必要的最小資料。
4. QR、分享連結、付款回呼不得暴露姓名、電話、Email 或可逆個資。
5. 金鑰、LINE token、LIFF secret、金流密鑰、Email key 僅走環境變數。
6. 所有 API 做輸入驗證、速率限制、錯誤處理與租戶邊界檢查。

---

## 11. 既有系統相容與遷移

1. 既有 `clinics`、`clinic_settings`、`patients`、`appointments`、LINE 與提醒流程必須持續可用。
2. 新功能以獨立 migration 加入；migration 必須可重跑、可檢查已存在欄位／索引／policy。
3. CRM Lite 的 migration、報名領域、付款領域、報表索引分開，避免一次失敗造成整個既有預約系統不可用。
4. 現有 `supabase/schema.sql` 必須同步包含新環境所需完整 schema；README 必須說明既有環境的 migration 順序。
5. 在 migration、RLS、資料隔離與回滾驗證完成前，不得宣稱 SaaS 已可正式服務多品牌。
6. 現行 `NEXT_PUBLIC_CLINIC_ID` 可作為相容部署 fallback；SaaS 正式模式必須使用登入後的品牌 context，不能只靠前端環境變數隔離。

---

## 12. 分階段開發交付

### Phase 0：規格與基礎邊界

- 以本 v3 凍結產品範圍與狀態定義。
- 更新 schema、migration strategy、RLS／角色矩陣與環境變數清單。
- 補齊報名、付款、報表的 API contract 與測試資料。

### Phase 1：SaaS 基礎與既有流程保護

- 品牌／租戶 context、成員與角色、品牌資料隔離。
- 既有預約、提醒、LIFF、後台功能回歸測試。
- 自訂網址與瀏覽器入口的路由邊界。

### Phase 2：報名與標準金流

- 活動／梯次／票種／表單／候補／QR 報到。
- 綠界／藍新標準付款、回呼冪等與付款狀態。
- 報名、付款、通知與報表的整合測試。

### Phase 3：CRM Lite、行銷與報表

- 顧客時間軸、分眾、三種規則式自動化、LINE／Email 投遞。
- 標準營運報表、CSV 匯出、PII 權限測試。
- Cron、重試、去重、opt-in 與退訂／封鎖測試。

### Phase 4：上線硬化與交付

- 多品牌隔離攻擊測試、RLS 測試、備份／還原演練、錯誤監控。
- 行動瀏覽器、LIFF、嵌入元件、自訂網域與 HTTPS 驗證。
- 操作手冊、`.env.example`、部署流程、驗收報告與已知限制。

---

## 13. 完整驗收標準

### 13.1 必過功能驗收

- 新品牌可建立預設設定、邀請成員、設定服務／活動與發布入口。
- 同一登入帳號可依授權切換品牌；任何 API、RLS、匯出與 URL 猜測都無法讀取未授權品牌資料。
- 時間制與號次制都能正確查空位、原子訂位、取消、改期、候補與狀態追蹤。
- 報名可完成公開／私密頁、表單、票種、名額、候補、付款與 QR 報到。
- 綠界／藍新付款回呼重送不會重複確認、扣款紀錄或占用名額。
- LINE、Email、瀏覽器入口均能依品牌設定運作，失敗有記錄且不會造成整批 Cron 中斷。
- CRM Lite 對 opt-in、分眾、生日、完成後追蹤、未回訪、冷卻與去重行為正確。
- 報表數字可用固定測試資料人工核算，CSV 與畫面結果一致。

### 13.2 必過安全驗收

- anon key 無法讀取任何顧客、預約、報名、付款、CRM 或投遞資料。
- 顧客端 bundle、network response、錯誤頁不包含 service role key、金流密鑰或不必要 PII。
- LIFF token、LINE webhook、付款 webhook 都會驗證，無效請求不會寫入資料。
- 所有跨品牌查詢、更新、刪除、匯出與背景工作都能被拒絕或限制在正確品牌。
- QR 與分享連結不可反推出 PII；狀態異動可追溯操作者與時間。

### 13.3 必過工程驗收

- `npm run typecheck` 通過。
- `npm run build` 通過。
- migration 可在全新資料庫與既有資料庫各自完成；重跑不破壞資料。
- schema、migration、README、`.env.example`、API contract 與測試資料同步。
- 主要流程有自動化測試；預約競爭、付款回呼重送、提醒／行銷去重與跨租戶拒絕有專門測試。
- 備份／還原與失敗排程重跑有實際紀錄，不能只以「程式看起來會運作」作為完成證明。

## 14. 上線前仍需由每個品牌提供的部署資料

這些不是產品範圍未定，而是正式上線時的品牌設定：

- 品牌名稱、Logo、色系、公開頁內容與服務／活動資料。
- LINE Official Account、Channel、LIFF 與 Rich Menu 設定。
- 綠界／藍新商店資料與 webhook 設定。
- Email 寄件網域、寄件名稱、通知與行銷模板。
- 自訂網址／網域所有權與 DNS／HTTPS 設定。
- CRM Lite 的實際分眾條件、訊息內容、發送渠道與品牌同意政策。

以上資料未提供時，系統可完成開發與測試環境驗收，但不得宣稱該品牌已完成正式上線。
