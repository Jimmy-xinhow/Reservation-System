讀 CLAUDE.md。釐清一個被誤導的問題:後台「診所設定/門診表」讀不到 clinic_settings。已確認三件事:
(a) 資料庫 RLS 正確 —— 用登入身分(auth.uid()=7c9f75dd-761d-41a9-8d13-fa83f908f3e1)直接查 clinic_members 撈得到對應那筆,auth_clinic_ids() 在登入身分下會回傳 087f6757...。
(b) clinic_settings 的 authenticated policy 已存在(for all to authenticated, using clinic_id in (select auth_clinic_ids()))。
(c) Railway 線上跑的就是最新 commit。

你上一次的「修正」只是讓今日約診頁用 settings?.booking_mode ?? 'time' 蓋掉讀不到的情況,並沒有真正修好讀取 —— 兩頁其實都讀不到設定。

請從這個方向查根因:後台頁面查 clinic_settings 時,用的 Supabase client 是否真的帶著登入使用者的 session(authenticated 身分)？很可能用到了未綁定使用者 session 的 client(導致 auth.uid() 為 null、RLS 擋下、回 0 rows)。
1. 找出 settings 頁、schedules 頁實際查 clinic_settings 用的是哪個 client、有沒有正確從 cookie 帶入登入 session。
2. 對照能在登入身分下正確讀取的頁面，看差異。
3. 真正修正成「以登入使用者的 authenticated session 查詢」，不要再用預設值掩蓋、也不要改用 service-role 繞過 RLS 來假裝修好。
修正後說明根因與改動，push 並回報 commit hash。