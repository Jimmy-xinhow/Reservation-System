讀 CLAUDE.md。後台有頁面回報「查無診所設定,請先於 Supabase insert 一筆 clinic_settings」,但 clinic_settings 確實存在(今日約診頁右上角能正確讀到「模式:時間制」),且 Railway 的 NEXT_PUBLIC_CLINIC_ID 與資料庫的 clinic id 一致(087f6757-d1b6-4c4f-a82d-8bb5bc7c4733)。

請排查:
1. 找出顯示這段紅字的是哪一頁、哪段程式碼(可能是門診表或診所設定頁)。
2. 比對它讀取 clinic_settings 的方式,跟「今日約診」頁能成功讀取的方式有何不同——重點看是用哪一種 client(authenticated 一般查詢 vs service-role)、查詢條件帶的 clinic_id 從哪來、以及 clinic_settings 的 RLS policy 是否涵蓋 authenticated 後台帳號讀取自己診所設定。
3. 很可能是 clinic_settings 少了一條讓登入櫃檯 SELECT 自己診所設定的 RLS policy,或這頁用錯 client。判斷出根因後修正,維持安全原則(病患端仍走 service-role、後台走 authenticated + 限自己 clinic_id),不要為了讓它能讀就放寬成全開。
4. 若需要補 RLS policy,把對應 SQL 一併更新進 supabase/schema.sql,並另外給我一段「可單獨在 Supabase SQL Editor 補跑」的 SQL。
修正後跑 tsc --noEmit 與 next build 確認沒壞,回報根因與改了什麼。